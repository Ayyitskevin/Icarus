# Status-card fixture

This dependency-free fixture keeps status mapping in a Node module and consumes it from a JSX component.

Run the behavioral test and component contract with:

```sh
node --test tests/status.test.mjs
node checks/component-contract.mjs
```
