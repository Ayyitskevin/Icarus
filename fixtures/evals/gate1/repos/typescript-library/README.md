# Clamp library fixture

This dependency-free TypeScript library exports a numeric `clamp` function. It keeps values inside an inclusive range and rejects inverted bounds.

Run the existing tests with:

```sh
node --test tests/clamp.test.ts
```
