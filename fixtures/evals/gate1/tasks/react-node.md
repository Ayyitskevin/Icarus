Repair the status mapping used by the component.

Allowed change: `src/status.mjs` only.

Online users must receive the `Online` label and `positive` tone. Offline users must receive the `Offline` label and `muted` tone. Preserve the existing exported function signatures. Use the repository's existing tests to verify the repair.
