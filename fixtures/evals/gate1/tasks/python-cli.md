Repair the command-line word counter.

Allowed change: `src/word_count.py` only.

The counter must treat every run of Unicode whitespace as a separator, ignore leading and trailing whitespace, and return zero for empty or whitespace-only input. Preserve the existing command-line interface and public function signatures. Use the repository's existing tests to verify the repair.
