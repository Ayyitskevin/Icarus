Repair the command-line word counter.

Allowed change: `src/word_count.py` only.

The counter must treat every run of Unicode whitespace as a separator, ignore leading and trailing whitespace, and return zero for empty or whitespace-only input. Set the function's one-line docstring exactly to `Return the number of whitespace-separated fields in text.` Preserve the existing command-line interface and public function signatures. Use the repository's existing tests to verify the repair.
