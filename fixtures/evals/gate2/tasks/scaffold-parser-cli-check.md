# Scaffold: parser CLI

Add a dependency-free command-line entry point named `cli` around the boolean
parser, with deterministic exit codes. Add its check beside the existing parser
check, following that check's naming.
