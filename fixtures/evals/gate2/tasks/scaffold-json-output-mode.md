# Scaffold: JSON output mode

Add a dependency-free JSON output mode to the entry point while preserving the
default text mode. Include a registered offline check named `test_json_output.py`,
in a new `tests` directory.
