# Scaffold: greeting command

Add a dependency-free command named `greet` that prints the repository greeting
read from the greeting file, keeping that file as the source of truth and
preserving its one-line rule. Add its check beside the existing check as
`checks/test_greet.py`.
