# Gate 1 credential-gated live 3/3 record — 2026-08-23

## Outcome

Gate 1's credential-gated live-evidence requirement passed 3/3 against three
disposable private GitHub repositories with Actions disabled. The one-shot
executor ended `succeeded`, and a fresh process replayed the byte-identical
terminal receipt without a credential. The operator explicitly overrode the
otherwise-required independent review for this slice.

This record authorizes no merge, deployment, force-push, reference update or
deletion, live-state migration, or unattended execution against an active
repository. The three pull requests remain drafts for inspection.

## Bound authority

- Profile: `gate1-live-3of3-20260823-v1`
- Profile digest: `f7a872963f86875e5797ddc1ec3190a63bfc64770643d3e112acf781638df3c4`
- Manifest digest: `1b79bf4249fcb42ef11cdb6abcc11a6cb498fd5d09fa9831a06cd05c4ccc4297`
- Provider: loopback Ollama, `qwen3.8:27b`, adapter
  `production-ollama-api-chat-v1`, zero captured token price
- Authorized effects only: Git object upload, absent-only reference creation,
  draft-pull-request creation, and immutable landing receipt
- Resume id: `e46eba7e-fe83-4b0d-80f7-9875498dc4aa`

## Case receipts

| Case | Run | Candidate | Draft pull request |
| --- | --- | --- | --- |
| TypeScript library | `f72e66ba-f9cb-44de-99b9-6df07ddb88ee` | `b3626c47c17494760835ebbf279685759be84345` | [#3](https://github.com/Ayyitskevin/icarus-gate1-typescript-library/pull/3) |
| Python CLI | `e5680010-1aff-41c4-b6ce-ef575dd09259` | `70a50ec5d7f3ee54a68112c1923da5ccd63e59cd` | [#1](https://github.com/Ayyitskevin/icarus-gate1-python-cli/pull/1) |
| React/Node | `3b610869-dae9-4e2a-843c-3c63eeca94c8` | `6645f24cda9dfb339eb8803e6b452dbf23b7bbd2` | [#1](https://github.com/Ayyitskevin/icarus-gate1-react-node/pull/1) |

Every run completed with passing, untruncated registered checks and the exact
expected changed paths. Every landing receipt records `draft: true`, the exact
candidate commit, a newly created absent-only `icarus/<run-id>` branch, and a
new pull request. The source checkouts remained clean and pinned.

## Re-runnable local evidence

The credential-free evidence remains under ignored, owner-only local state:

```text
.local/gate1-live/execution-20260823T185833Z.ndjson
.local/gate1-live/replay-e46eba7e-fe83-4b0d-80f7-9875498dc4aa.ndjson
~/.local/state/icarus-gate1-live/state
```

The final line of both NDJSON files is the same canonical terminal receipt with
`outcome: "succeeded"` and all three case ids. The replay command is:

```bash
ICARUS_HOME="$HOME/.local/state/icarus-gate1-live/state" \
  node packages/cli/dist/main.js live-evidence resume \
  e46eba7e-fe83-4b0d-80f7-9875498dc4aa \
  --input .local/gate1-live/profile-approved.v1.json \
  --manifest .local/gate1-live/manifest.v1.json \
  --runs .local/gate1-live/runs.v1.json
```

`pnpm check` then exited 0 on the resulting tree: 1,060 unit/provider tests,
157 integration tests, the 3/3 offline Gate 1 benchmark, 183 security tests,
workflow lint, format, lint, typecheck, eval, and builds all passed. The live
3/3 evidence is deliberately not rerun by `pnpm check`.

## Next gate

Run one explicitly approved, supervised canary against a non-production active
repository before lifting the hold on unattended live execution. Keep merge and
deployment authority outside Icarus until their own accepted contracts and
evidence exist.
